[client-server]

# !includedir /etc/mysql/conf.d/
# !includedir /etc/mysql/mariadb.conf.d/

[mysqld]

# Repertoire pour db
datadir = /var/lib/mysql

# Pour que MariaDB accepte toute les connexions
bind_address = *

port = 3306

user = mysql